package Knumber

// K数，无序数组，某个数他的左边的所有数比他小，他右边的所有数比他大，他就是K数。K数不止一个。
// 例如[7, 4, 2, 8, 9, 12]，8，9是K数。

func FindK(nums []int) []int {
	length := len(nums)
	res := []int{}

	for index := range nums {

		i, j := index+1, length-1

	}

	return res
}
